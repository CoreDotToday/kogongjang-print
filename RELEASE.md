# 감사장 인쇄 시스템 v0.2.0

**릴리즈 일자: 2026-04-23**

이전 안정 버전(v0.1.0) 이후 약 7주간의 작업을 모은 릴리즈입니다.
설정 화면을 사용성 측면에서 크게 보강했고, 운영 중에 마주쳤던 안정성
이슈 두 건(설정 파일 손상, 키오스크 미종료)을 함께 해결했습니다.

---

## 한눈에 보기

- **라이브 미리보기 패널** — 배경/폰트/위치를 만지면 그 자리에서 결과가 보입니다.
  더 이상 인쇄해 보지 않아도 됩니다.
- **사용자 폰트 추가** — 원하는 TTF/OTF를 GUI에서 바로 등록해 미리보기와
  실제 인쇄 모두에 사용할 수 있습니다.
- **`data/` 폴더로 파일 정리** — exe 옆에 흩어져 있던 설정·배경·로그 파일을
  한 폴더로 모았습니다. 기존 사용자는 첫 실행 시 자동으로 옮겨갑니다.
- **이중 실행 시 부드러운 인계** — 이전 인스턴스를 정상 종료 후 새로 시작하거나,
  응답이 없으면 안전 검증 후 강제 종료할 수 있습니다.
- **앱 정보 표시** — 우하단 "정보" 버튼으로 버전·시스템 정보·릴리즈 노트를 확인.
- **설정 파일이 더 이상 깨지지 않음** — 정전·강제 종료에도 안전한 원자적 저장.

---

## 새 기능

### 라이브 미리보기 패널
메인 창 우측에 A4 비율 썸네일이 항상 표시됩니다. 배경 템플릿, 폰트,
글자 크기, 이름 위치(X/Y/W/H) — 어떤 설정을 바꿔도 약 0.3초 후 자동으로
다시 그려집니다. 샘플 이름은 별도 입력칸에 입력할 수 있고, 기본값은 "홍길동".
"텍스트 영역 표시" 토글을 켜면 이름이 들어갈 박스가 빨간 테두리로 표시되어
좌표 조정이 한결 직관적입니다.

### 사용자 폰트 추가
폰트 드롭다운 옆 "추가" 버튼으로 TTF/OTF 파일을 선택하면
`data/fonts/` 폴더에 복사되어 즉시 목록에 나타납니다. 미리보기와
실제 PDF 인쇄 모두 동일한 lookup 경로를 사용하므로 추가 즉시 결과에 반영됩니다.

### 이중 실행 처리
이미 실행 중인 상태에서 다시 실행하면 "이미 실행 중" 안내 대신 다음 셋 중에서 선택:

- **정상 종료 후 시작** — 기존 인스턴스에 정상 종료를 요청하고 (키오스크
  Chrome 포함) 포트가 비면 새로 시작합니다.
- **강제 종료 후 시작** — 응답이 없거나 비상 상황일 때 사용. 안전을 위해
  대상 프로세스가 감사장 인쇄 프로그램(`감사장인쇄.exe` / `python.exe` /
  `pythonw.exe`)인지 자동 검증한 뒤에만 강제 종료합니다. 다른 프로그램이
  같은 포트를 점유 중이면 거부 메시지를 띄웁니다.
- **취소** — 새 인스턴스를 띄우지 않고 종료.

### 앱 정보 다이얼로그
창 하단 상태바의 "정보" 버튼으로 다음을 확인할 수 있습니다:

- 앱명 / 버전 / 릴리즈 일자 / 저작권
- 시스템 정보 (Python·플랫폼·실행 모드)
- **릴리즈 노트** 탭 — 전체 변경 이력을 그 자리에서 스크롤해 확인 가능

빌드된 exe는 Windows 탐색기 → 우클릭 → 속성 → 세부 정보 탭에서도
제품명·버전 메타데이터를 볼 수 있습니다.

---

## 안정성 개선

### 설정 파일 손상으로 프로그램이 안 켜지던 문제 해결
정전이나 강제 종료가 발생하면 기존에는 `config.json`이 NUL 바이트로
채워진 상태로 남아 다음 실행 시 프로그램이 크래시했습니다. 이제는:

- 저장 시 임시 파일에 먼저 쓰고 디스크 sync 후 원자적으로 교체합니다.
  전원이 언제 끊겨도 설정 파일은 항상 "이전 정상본" 또는 "새 정상본" 둘 중
  하나로만 남습니다.
- 혹시 이전 버전에서 깨진 파일이 남아 있더라도 자동으로 `config.json.corrupt`로
  보존하고 기본값으로 복구합니다 — 사용자가 수동으로 파일을 지울 필요가 없어요.

### 키오스크 종료가 확실히 되도록 수정
`/close-kiosk` API 또는 키오스크 종료 버튼이 Chrome 메인 프로세스만
종료하고 자식 프로세스(GPU/렌더러/네트워크 등)는 남기는 경우가 있었습니다.
이제 `taskkill /F /T`로 프로세스 트리 전체를 강제 종료해 화면이 확실히 닫힙니다.

---

## 파일 구조 변경

운영 중 exe 폴더가 어수선해지는 문제를 정리했습니다. 모든 사용자 파일을
`data/` 한 폴더로 모았습니다:

```
감사장인쇄.exe
data/
  ├── config.json     ← 설정
  ├── backgrounds/    ← 추가한 배경 PNG
  ├── fonts/          ← 추가한 폰트 (이번 릴리즈에서 신설)
  ├── output/         ← 생성된 PDF
  └── error.log       ← 오류 로그
```

### 업그레이드 안내

기존 사용자는 **별도로 할 일이 없습니다**. v0.1.x를 쓰던 PC에서 새 exe를
처음 실행하면, 다음 파일들이 자동으로 `data/` 안으로 이동됩니다:

- `config.json` → `data/config.json`
- `backgrounds/` 폴더 전체 → `data/backgrounds/`
- `output/` 폴더 전체 → `data/output/`

`data/`에 같은 이름이 이미 있으면 이동하지 않습니다 (기존 파일 보호).
설정·등록한 배경은 그대로 유지됩니다.

---

## 새 API

- `POST /quit` — 앱(서버 + GUI + 키오스크) 정상 종료. **PC 전원에는 영향 없음.**
  (PC 전원 종료는 기존 `POST /shutdown`이 그대로 담당합니다 — 이름이 비슷하니
  주의하세요.)

기존 API(`/preview`, `/test`, `/print`, `/close-kiosk`, `/shutdown`,
`/backgrounds/<filename>`)는 변경 없이 동일하게 동작합니다.

---

## 참고

- 자세한 변경 내역은 [CHANGELOG.md](CHANGELOG.md) 참고.
- 설치/빌드 방법은 README와 `command` 파일 참고.
- 개발자가 코드를 수정할 경우 `CLAUDE.md`의 아키텍처 설명 참고.
